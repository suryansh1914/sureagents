---
name: sureagents-annotate
disable-model-invocation: true
description: Open SureAgents's annotation UI for a file, folder, or URL, then address the returned annotations.
---

# SureAgents Annotate (Kiro)

Run:

```bash
SUREAGENTS_ORIGIN=kiro-cli sureagents annotate $ARGUMENTS
```

`$ARGUMENTS` should be a markdown file path, folder path, html file path, or URL.
